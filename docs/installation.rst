============
Installation
============

At the command line::

    $ easy_install django-waffle-cbv

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-waffle-cbv
    $ pip install django-waffle-cbv
